// Re-export from context to maintain backwards compatibility
// This file is kept for existing imports but now uses the centralized context
export { useGeolocation } from '@/lib/geolocation-context'
